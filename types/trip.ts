export interface Trip {
  tripName: string;
  country: string;
  city: string;
  startDate: string;
  endDate: string;
  people: string;
}

export interface Schedule {
  id: string;
  title: string;
  location: string;
  date: string;
  time: string;
}